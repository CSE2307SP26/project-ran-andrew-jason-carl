package main;

// checking vs savings accounts 
// savings account has a balance requirement, a higher interest rate and a withdrawal fee after first 6 withdrawals
public enum AccountType {
    CHECKING,
    SAVINGS,
}
